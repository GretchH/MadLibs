import React from 'react';

const Input = (title) => {
   return (
       <div className="input">
           <input />
           <label>{title}</label>
       </div>
   ) 
}
// t

export default Input;